/**
 * Test Fixtures
 * Central export for all test fixture data
 */

const { mockUsers } = require('./users');
const { mockRequests, mockStatuses } = require('./requests');

module.exports = {
  mockUsers,
  mockRequests,
  mockStatuses,
};
