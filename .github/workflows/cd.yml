name: 🚀 Deploy GatePass to Production (CD)

on:
  workflow_run:
    workflows: ["🧪 Build & Test GatePass (CI)"]
    branches: [main]
    types:
      - completed

jobs:
  deploy:
    name: Production Deployment
    runs-on: ubuntu-latest

    # Run ONLY if CI succeeded
    if: ${{ github.event.workflow_run.conclusion == 'success' }}

    steps:
      # ==================================================
      # CHECKOUT EXACT CI COMMIT
      # ==================================================
      - name: ⬇️ Checkout CI commit
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event.workflow_run.head_sha }}

      # ==================================================
      # NODE SETUP
      # ==================================================
      - name: 🛠️ Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      # ==================================================
      # FRONTEND BUILD
      # ==================================================
      - name: 🏗️ Build frontend
        working-directory: frontend
        run: |
          npm ci
          npm run build

      # ==================================================
      # VERIFY FRONTEND DIST
      # ==================================================
      - name: ✅ Verify frontend build output
        run: |
          test -d frontend/dist || { echo "❌ dist missing"; exit 1; }
          test -f frontend/dist/index.html || { echo "❌ index.html missing"; exit 1; }
          test -d frontend/dist/assets || { echo "❌ assets missing"; exit 1; }
          echo "✅ frontend build verified"

      # ==================================================
      # VERIFY BACKEND
      # ==================================================
      - name: ✅ Verify backend
        run: |
          test -d backend || exit 1
          test -f backend/server.js || exit 1
          test -f backend/package.json || exit 1
          echo "✅ backend verified"

      # ==================================================
      # UPLOAD TO SERVER
      # ==================================================
      - name: 🚚 Upload artifacts
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USERNAME }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          port: 22
          source: ./*
          target: /home/dpd/GatePass-deploy
          rm: true

      # ==================================================
      # DEPLOY
      # ==================================================
      - name: 🔐 Deploy on production server
        uses: appleboy/ssh-action@v0.1.10
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USERNAME }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          port: 22
          script: |
            set -euo pipefail

            echo "🚀 Starting GatePass deployment"

            # =====================
            # Paths
            # =====================
            RELEASE_TIME=$(date +"%Y%m%d-%H%M%S")
            RELEASE_DIR="/var/www/releases/release-$RELEASE_TIME"
            CURRENT_DIR="/var/www/releases/current"

            FRONTEND_ROOT="/opt/Gate_Pass"
            FRONTEND_BLUE="/opt/Gate_Pass_blue"
            FRONTEND_GREEN="/opt/Gate_Pass_green"

            mkdir -p "$RELEASE_DIR"
            cp -r /home/dpd/GatePass-deploy/* "$RELEASE_DIR/"
            ln -sfn "$RELEASE_DIR" "$CURRENT_DIR"

            # =====================
            # Backend
            # =====================
            cd "$CURRENT_DIR/backend"
            npm ci --omit=dev

            if pm2 describe Gate-Pass >/dev/null; then
              pm2 reload Gate-Pass --update-env
            else
              pm2 start server.js --name Gate-Pass
            fi
            pm2 save

            # =====================
            # Frontend (BLUE → GREEN)
            # =====================
            mkdir -p "$FRONTEND_GREEN"
            rm -rf "$FRONTEND_GREEN"/*
            cp -r "$CURRENT_DIR/frontend/dist/"* "$FRONTEND_GREEN/"

            # Atomic switch
            ln -sfn "$FRONTEND_GREEN" "$FRONTEND_ROOT"

            # =====================
            # Health Check (Backend)
            # =====================
            sleep 5
            if ! curl -sf http://127.0.0.1:5000/api/health > /dev/null; then
              echo "❌ Health check failed — rolling back"

              ln -sfn "$FRONTEND_BLUE" "$FRONTEND_ROOT"

              PREVIOUS=$(ls -dt /var/www/releases/release-* | sed -n '2p')
              if [ -n "$PREVIOUS" ]; then
                ln -sfn "$PREVIOUS" "$CURRENT_DIR"
                pm2 reload Gate-Pass
              fi

              exit 1
            fi

            # =====================
            # Promote GREEN → BLUE
            # =====================
            rm -rf "$FRONTEND_BLUE"
            cp -r "$FRONTEND_GREEN" "$FRONTEND_BLUE"

            echo "🎉 Deployment successful!"
